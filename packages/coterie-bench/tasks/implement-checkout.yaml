id: implement-checkout
category: tournament
prompt: "Implement POST /api/v2/checkout. High-stakes; want best of N."
expected_modes:
  - tournament
  - adversarial
workdir_setup:
  spec.md: |
    # /api/v2/checkout

    Idempotency-key required. Returns 201 on success, 409 on duplicate.
graders:
  - kind: status_done
