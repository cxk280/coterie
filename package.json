{
  "name": "coterie-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo root for Coterie — a LangGraph orchestrator for heterogeneous coding agents.",
  "license": "MIT",
  "workspaces": [
    "packages/coterie-js",
    "packages/coterie-web"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "test": "npm run -ws test",
    "lint": "npm run -ws lint"
  }
}
