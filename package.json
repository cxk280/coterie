{
  "name": "coterie-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Repo root for Coterie — a conversational multi-agent coding CLI. The package lives in packages/coterie-js.",
  "license": "MIT",
  "workspaces": [
    "packages/coterie-js"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "test": "npm run -ws test",
    "lint": "npm run -ws lint"
  }
}
